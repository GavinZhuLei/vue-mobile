import {
  /* eslint-disable no-unused-vars */
  Style,
  Button,
  Toast,
  Scroll,
  createAPI,
  BetterScroll
  // TabPanel
} from 'cube-ui'

export default Vue => {
  Vue.use(Button)
  Vue.use(Toast)
  Vue.use(Scroll)
}
